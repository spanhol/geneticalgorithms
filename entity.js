class Entity {
    /**
     * @param {number} x 
     * @param {number} y 
     * @param {Array} dna 
     */
    constructor(x, y, dna) {
        this.x = x;
        this.y = y;
        this.dna = dna;
    }
}